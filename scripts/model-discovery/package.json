{
	"name": "@ai-stats/model-discovery",
	"version": "1.0.0",
	"type": "module",
	"private": true,
	"scripts": {
		"check": "tsx check-new-models.ts",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {
		"@aws-sdk/client-bedrock": "^3.700.0"
	},
	"devDependencies": {
		"vitest": "^2.1.9"
	}
}
