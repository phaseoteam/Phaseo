// src/lib/providers/x-ai/index.ts
// Purpose: Provider adapter module for x-ai.
// Why: Isolates provider-specific configuration and utilities.
// How: Defines provider-specific endpoint adapters and configuration helpers.

// xAI (Grok) is fully OpenAI-compatible and uses the openai_compat executor
// No custom adapter needed - everything goes through IR pipeline
// Registered in providers/index.ts to use createOpenAICompatibleAdapter("x-ai")









