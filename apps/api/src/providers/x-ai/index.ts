// src/lib/providers/x-ai/index.ts
// xAI (Grok) is fully OpenAI-compatible and uses the openai_compat surface
// No custom adapter needed - everything goes through IR pipeline
// Registered in providers/index.ts to use createOpenAICompatibleAdapter("x-ai")
