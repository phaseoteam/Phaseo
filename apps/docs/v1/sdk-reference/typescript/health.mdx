---
title: "Health"
description: "Call /health with the TypeScript SDK."
---

**Method**: `client.getHealth()`.

### Example
```typescript
const health = await client.getHealth();
```

### Returns

```json
{
  "status": "ok"
}
```
