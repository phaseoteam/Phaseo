---
title: "Audio speech"
description: "Python SDK placeholder for /audio/speech."
---

<Note type="warning">
  This helper currently returns a `coming_soon` placeholder in the Python SDK.
</Note>

**Method**: `client.generate_speech()`

```python
result = client.generate_speech({
    "model": "openai/gpt-4o-mini-tts",
    "input": "Hello world",
})

print(result)
# {
#   "status": "coming_soon",
#   "endpoint": "audio/speech",
#   "message": "This endpoint is not yet supported in the SDK.",
#   "payload": {...}
# }
```
