---
title: "Audio translations"
description: "Python SDK placeholder for /audio/translations."
---

<Note type="warning">
  This helper currently returns a `coming_soon` placeholder in the Python SDK.
</Note>

**Method**: `client.generate_translation()`

```python
result = client.generate_translation({
    "model": "openai/gpt-4o-transcribe",
    "audio_b64": "base64-audio",
})

print(result)
# {
#   "status": "coming_soon",
#   "endpoint": "audio/translations",
#   "message": "This endpoint is not yet supported in the SDK.",
#   "payload": {...}
# }
```
