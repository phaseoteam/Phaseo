---
title: "Audio transcriptions"
description: "Python SDK placeholder for /audio/transcriptions."
---

<Note type="warning">
  This helper currently returns a `coming_soon` placeholder in the Python SDK.
</Note>

**Method**: `client.generate_transcription()`

```python
result = client.generate_transcription({
    "model": "openai/gpt-4o-transcribe",
    "audio_b64": "base64-audio",
})

print(result)
# {
#   "status": "coming_soon",
#   "endpoint": "audio/transcriptions",
#   "message": "This endpoint is not yet supported in the SDK.",
#   "payload": {...}
# }
```
