---
title: "Image generations"
description: "Call /images/generations with the Python SDK."
---

**Method**: `client.generate_image()`

```python
result = client.generate_image({
    "model": "openai/gpt-image-1",
    "prompt": "A lighthouse at golden hour",
})

print(result)
```
