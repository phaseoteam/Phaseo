---
title: "Image edits"
description: "Python SDK placeholder for /images/edits."
---

<Note type="warning">
  This helper currently returns a `coming_soon` placeholder in the Python SDK.
</Note>

**Method**: `client.generate_image_edit()`

```python
result = client.generate_image_edit({
    "model": "openai/gpt-image-1",
    "prompt": "Make it sunset",
    "image": "data:image/png;base64,...",
})

print(result)
# {
#   "status": "coming_soon",
#   "endpoint": "images/edits",
#   "message": "This endpoint is not yet supported in the SDK.",
#   "payload": {...}
# }
```
