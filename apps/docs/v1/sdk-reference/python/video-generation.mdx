---
title: "Video generation"
description: "Python SDK placeholder for /videos."
---

<Note type="warning">
  This helper currently returns a `coming_soon` placeholder in the Python SDK.
</Note>

**Method**: `client.generate_video()`

```python
result = client.generate_video({
    "model": "openai/gpt-video-1",
    "prompt": "A calm ocean at dusk",
})

print(result)
# {
#   "status": "coming_soon",
#   "endpoint": "videos",
#   "message": "This endpoint is not yet supported in the SDK.",
#   "payload": {...}
# }
```
