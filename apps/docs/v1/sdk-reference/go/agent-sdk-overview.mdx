---
title: "Overview"
description: "What the Go Agent SDK is for and what surface it currently exposes."
---

The Go Agent SDK is a minimal local runtime for AI Stats Gateway.

Use it when you want:

- a lightweight bounded tool loop
- gateway-backed `responses` requests
- app-owned control over persistence and orchestration

## What it ships

- `CreateAgent(...)`
- `DefineTool(...)`
- `CreateGatewayAgentClient(...)`
- a bounded tool loop over the AI Stats `responses` API

## Next

- [Installation](./agent-sdk-installation.mdx)
- [Usage](./agent-sdk.mdx)
