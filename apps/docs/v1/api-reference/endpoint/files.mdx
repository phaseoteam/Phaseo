---
title: "Files (Beta)"
badge: "Beta"
openapi: "POST /files"
description: "Upload files for use with batch jobs; list and retrieve existing uploads."
---

Related endpoints:

- `GET /files` to list uploads.
- `GET /files/{file_id}` to retrieve a single file's metadata.

