---
title: "Video generation (Beta)"
badge: "Beta"
openapi: "POST /videos"
---
Creates an asynchronous video generation job.

After creating a job, poll:
- `GET /v1/videos/{video_id}` for status updates.
- `GET /v1/videos/{video_id}/content` when the job is complete.

