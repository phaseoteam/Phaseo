---
title: "Gateway API Overview"
sidebarTitle: "Overview"
description: "Base URL, authentication, and endpoint families for the Phaseo Gateway."
icon: "map"
---

Use this page to find the correct API path before opening an endpoint reference. All Phaseo Gateway endpoints share one base URL and use bearer authentication.

## Choose your path

| If you want to | Start with |
| --- | --- |
| Send your first request | [Quickstart](../quickstart) |
| Generate text or use tools | [`POST /responses`](./endpoint/responses) |
| Keep an existing OpenAI-compatible integration | [`POST /chat/completions`](./endpoint/chat-completions) |
| Browse models and providers | [`GET /models`](./endpoint/models) and [`GET /providers`](./endpoint/providers) |
| Create music or manage files | [`POST /music/generations`](./endpoint/music-generate) and [`POST /files`](./endpoint/files) |
| Generate video (Beta) | [`POST /videos`](./endpoint/video-create) |
| Submit a batch or download results (Beta) | [`POST /batches`](./endpoint/batches-create) and [`GET /batches/{batch_id}/results`](./endpoint/batches-results) |

## Base URL

Production base URL:

```http
https://api.phaseo.app/v1
```

All endpoint paths in this reference are relative to that `/v1` base URL.

## OpenAPI

Use the OpenAPI file if you want to import the API into Postman, Hoppscotch, or an SDK generator.

<Card
	title="Phaseo Gateway OpenAPI"
	icon="list-bullet"
	href="https://github.com/phaseoteam/Phaseo/blob/main/apps/docs/openapi/v1/openapi.yaml"
>
	View the specification file
</Card>

The documentation and SDKs use this same specification. Video and Batch endpoints are labelled **Beta**; workspace preview access is still required.

## Authentication

Use a standard Gateway API key for inference and a management API key with the appropriate permissions for administration. See [Authentication](../developers/authentication.mdx) and [Management API Keys](../developers/management-api-keys.mdx).

```http
Authorization: Bearer phaseo_v1_sk_<kid>_<secret>
```

If authentication fails, the API returns `401`.

## API resources

| Resource | What it does |
| --- | --- |
| Inference | Generate text, images, audio, and embeddings; rerank, moderate, or parse content. Start with [Responses](./endpoint/responses.mdx). |
| Async Jobs & Files | Submit [video](./endpoint/video-create.mdx) or [batch](./endpoint/batches-create.mdx) jobs, transfer files, and configure webhooks. |
| Models & Pricing | Discover [models](./endpoint/models.mdx), provider offers, and [estimated costs](./endpoint/pricing-calculate.mdx). |
| Routing & Policies | Configure presets, provider credentials, private models, and [guardrails](./endpoint/guardrails.mdx). |
| Usage & Monitoring | Inspect [logs](./endpoint/logs-list.mdx), activity, analytics, evaluations, and service health. |
| Workspace Management | Manage [workspaces](./endpoint/workspaces-list.mdx), keys, budgets, identities, and integrations. |

## Billing and rate limits

- Handle provider throttling and applicable access policies using the [rate-limit guide](./limits.mdx).
- Paid requests require enough wallet balance to pass the preflight credit check.
- Free models with `:free` do not require deposited credits.
