---
title: "Codex"
description: "Use the AI Stats Gateway as a custom provider for Codex CLI, Codex App, and the Codex IDE extension."
---

Codex supports custom model providers through `config.toml`, so you can route Codex requests through AI Stats while keeping the same CLI, app, or IDE workflow.

If you also want Codex to read the live AI Stats documentation while it works, add the [Docs MCP](./docs-mcp.mdx) server as well.

<Visibility for="humans">
<Note>
Need reusable agent setup help as well as provider wiring? Install the public AI Stats skills from [`AI-Stats/skills`](https://github.com/AI-Stats/skills):

```bash
npx skills add AI-Stats/skills --agent codex --skill '*'
```
</Note>
</Visibility>

<Visibility for="agents">
<Note>
Reusable AI Stats skills are available from [`AI-Stats/skills`](https://github.com/AI-Stats/skills). For Codex installs, use:

```bash
npx skills add AI-Stats/skills --agent codex --skill '*'
```
</Note>
</Visibility>

## Prerequisites

- An AI Stats API key.
- Codex CLI, Codex App, or the Codex IDE extension.

## 1) Set your API key

```bash
export AI_STATS_API_KEY="your_ai_stats_api_key"
```

```powershell
$env:AI_STATS_API_KEY = "your_ai_stats_api_key"
```

## 2) Configure Codex to use AI Stats

Create or update your user config at `~/.codex/config.toml`. For a trusted project, you can also place the same settings in `.codex/config.toml` inside the repo.

```toml
model = "openai/gpt-5.5"
model_provider = "ai_stats"

[model_providers.ai_stats]
name = "AI Stats"
base_url = "https://api.phaseo.app/v1"
env_key = "AI_STATS_API_KEY"
wire_api = "responses"
```

Codex currently uses the Responses wire protocol for custom providers. Keep `wire_api = "responses"` or omit it and use the default.

## 3) Start Codex

```bash
codex
```

The Codex App and IDE extension share the same config layers. In the IDE extension, open the config from `Codex Settings > Open config.toml`.

## Verification

- Confirm your model id exists in [Models](../../api-reference/endpoint/models.mdx) before launching.
- Use [Responses](../../api-reference/endpoint/responses.mdx) for the default Codex-style flow.

## Troubleshooting

- `401 Unauthorized`: check that `AI_STATS_API_KEY` is set in the same shell running `codex`.
- `model_not_found`: fetch a current id from [Models](../../api-reference/endpoint/models.mdx) and update `model`.
- Provider-specific issues: follow [Routing and fallbacks](../routing-and-fallbacks.mdx) for failover behavior.
