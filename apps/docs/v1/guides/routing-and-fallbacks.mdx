---
title: "Routing and Fallbacks"
description: "How the Gateway selects providers and keeps requests reliable."
icon: "git-branch"
---

The Phaseo Gateway routes each request to a provider that can serve your chosen model. When a provider is slow, rate-limited, or returning errors, the Gateway can attempt fallbacks so your requests still complete.

## Choose a routing mode

Start with `balanced` unless one production requirement clearly matters more than the others.

| Mode | Use it when |
| --- | --- |
| `balanced` | You want a practical mix of price, latency, throughput, and availability. |
| `price` | Reducing provider cost matters more than minimizing response time. |
| `latency` | Fast response start is the primary requirement. |
| `throughput` | Sustained token generation speed matters most. |

Configure the workspace default in **Dashboard -> Settings -> Routing**. Use presets when one workflow needs a narrower provider or model policy than the workspace default.

### Model routing suffixes

Append a routing suffix when the model ID itself should make the optimization mode authoritative for that request:

| Suffix | Routing mode |
| --- | --- |
| `:nitro` | `throughput` |
| `:cheap` | `price` |
| `:fast` | `latency` |

For example, `openai/gpt-5-mini:nitro` uses throughput-first routing. A recognized routing suffix takes precedence over request-level `routing.mode` or `provider.sort`, preset and workspace routing modes. Other constraints such as provider allow lists, regional requirements, guardrails, and price ceilings still apply.

## How routing works at a high level

- You send a request with a model id.
- The Gateway evaluates provider health, latency, and capability coverage.
- A provider is selected and the request is executed.

Inspect request outcomes through your activity logs and response metadata when debugging routing behavior.

## Select a model with the auto-router

Auto Routing is currently an Alpha available to selected workspaces.

Use `phaseo/auto` when the model should change with the workload. Phaseo starts
from every eligible production text model, then applies workspace constraints
and builds a workload-specific shortlist:

1. Open **Dashboard -> Settings -> Routing -> Auto Routing**.
2. Choose whether to optimize for balanced performance, quality, cost, or
   latency.
3. Select an Economy, Standard, Premium, or unrestricted spend profile. These
   profiles apply fixed input and output price ceilings before scoring.
4. Optionally narrow eligible models with patterns such as `anthropic/*`,
   `openai/gpt-5.*`, or an exact model ID.
5. Choose whether Phaseo may try the remaining ranked models after a retryable
   failure.
6. Save the configuration.

Applications then opt in without copying workspace routing policy into every
request:

```json
{
  "model": "phaseo/auto",
  "input": "Review this TypeScript function for correctness."
}
```

The request cannot change the workspace objective, spend profile, or model
patterns. Fixed model requests continue to bypass the auto-router.

The objective changes the relative weight of model quality, provider
reliability, latency, and price:

| Objective | Use it when |
| --- | --- |
| `balanced` | You want a practical default across all four signals. |
| `quality` | Relevant benchmark performance matters most. |
| `cost` | Lower estimated input and output token price matters most. |
| `latency` | Lower recent provider latency matters most. |

Spend profiles are hard standard-tier price ceilings in USD per million text
tokens:

| Spend profile | Maximum input price | Maximum output price |
| --- | ---: | ---: |
| Economy | $0.10 | $0.50 |
| Standard | $0.30 | $1.50 |
| Premium | $1 | $5 |
| Any price | No ceiling | No ceiling |

Custom limits set the input and output ceilings directly. Models without known
standard text pricing are not included in the managed candidate universe.

For each `phaseo/auto` request, Phaseo creates a normal child gateway request
to a pinned low-cost classifier model. The child request uses the same
workspace and billing identity, appears separately in request logs, and is
labelled with `purpose=auto_routing_classifier` and the parent request ID.
The classifier returns a structured workload mix, complexity score, and
confidence value; it never selects a model directly.

Hard request facts such as tools and structured output are included as trusted
metadata. If the classifier request fails, times out, or returns invalid data,
Phaseo falls back to the local deterministic code, reasoning, tool use,
structured output, translation, summarization, or general classifier rather
than failing the generation request.

Classifier complexity represents the minimum model capability likely to
produce a reliably acceptable answer. Phaseo applies a capability margin, then
combines benchmark fit with the workspace objective, price, latency, provider
health, and reliability. The classifier request and selected generation
request are metered independently through the normal gateway pipeline.

Before scoring, every allow-listed model must pass the normal endpoint,
workspace model, provider, privacy, guardrail, and circuit-breaker checks. The
router then combines relevant non-self-reported Phaseo catalogue benchmarks
with current Phaseo provider health, latency, and pricing. Missing benchmark or
operational data is neutral; it does not widen the allow-list.

The response `model` field identifies the selected model. Request details show
the workload, objective, selected model, fallback order, candidate scores,
factor scores, benchmark IDs, exclusions, and algorithm version. Routing traces
contain no prompt or response content.

If model fallbacks are enabled in the workspace, Phaseo retries the remaining ranked models on
`429`, `500`, `502`, `503`, or `504`. Each fallback passes the complete policy
and provider-selection pipeline again. Client errors do not switch models.

A fixed model selected by an attached dynamic route takes precedence. In that
case, request details record the override and auto-router model fallbacks are
disabled for the request.

<Warning>
  Spend profiles and model patterns are eligibility controls, not quality
  guarantees. Validate the resulting model mix against your own workloads.
</Warning>

## Explain a routing decision

Open **Dashboard -> Settings -> Usage -> Request logs**, select a request, then expand **Routing observability** under **Provider Responses**.

The request log shows:

- every ranked provider and its final score
- the provider Phaseo selected and any providers it attempted
- providers excluded before ranking, with the recorded reason
- providers that were deranked by their rollout or routing status
- the score inputs, weights, contributions, and multipliers used for each ranked provider

Score factors are separated from recorded context. A score factor changes the final score for the active routing mode. Recorded context helps explain the decision but does not necessarily affect the score.

For `balanced` routing, Phaseo scores the eligible providers using reliability, latency, tail latency, throughput, price, and token fit. The displayed calculation shows how each factor contributed to the final score instead of presenting every recorded metric as equally important.

### Reliability and provider uptime

The reliability sample is the value used by the score. It is calculated from provider outcomes; the success rate is shown as supporting context.

These outcomes reduce provider uptime:

- authentication failures (`401`)
- payment failures (`402`)
- model-not-found responses (`404`)
- server errors (`500` and above)
- errors after a response stream starts
- successful HTTP responses that finish with an error reason

These outcomes do not reduce provider uptime:

- bad requests (`400`)
- geographic restrictions (`403`)
- oversized payloads (`413`)
- rate limits (`429`)

Geographic restrictions and rate limits are tracked separately because they do not show that the provider itself is unavailable.

### Trace availability and privacy

Complete routing traces are available for requests made after routing observability was enabled. Older requests may show a partial trace or no routing details.

Routing traces are bounded and content-free. They contain the numbers and statuses needed to explain provider selection, but do not copy prompts, messages, or generated content into the trace.

## Choose an exact provider in the model id

Use `<provider-id>:<canonical-model-id>` when a request must use one specific provider-model pair:

```json
{
  "model": "baseten:thinking-machines/inkling-small",
  "input": "Hello"
}
```

The qualifier disables cross-provider fallback for that request. Model suffixes remain part of the canonical model ID, including in identifiers such as `baseten:google/gemma-4-26b-a4b:free`.

Read [Provider-qualified model IDs](./provider-qualified-models) for complete syntax, free-route validation, routing precedence, aliases, error codes, and request examples.

## Control routing and fallbacks

The current public fallback and routing controls are intentionally explicit:

### Presets constrain the fallback pool

In **Dashboard -> Settings -> Presets**, you can define:

- allowed models
- provider allow lists
- provider ignore lists
- default prompt and parameter behavior

Those constraints are applied before provider selection, so a preset can intentionally narrow which providers are eligible for retries and failover.

### Routing mode changes provider ranking

In **Dashboard -> Settings -> Routing**, workspaces can tune how the Gateway ranks compatible providers:

- `balanced`
- `price`
- `latency`
- `throughput`

The same page also exposes beta and alpha channel toggles so preview traffic can be introduced intentionally instead of appearing as an untracked routing side effect.

### BYOK fallback is explicit

In **Dashboard -> Settings -> BYOK**, teams can choose whether a failed BYOK request is allowed to fall back to Phaseo credits. This is the current public control for the common "my own key failed, should the request still complete?" decision.

### Dynamic routes attach policy to API keys

In **Dashboard -> Settings -> Routing**, create a dynamic route when different
API keys or request classes need different provider behavior. A route can:

- branch on nested request-body fields, request headers, custom metadata,
  endpoint, model, or session ID
- split traffic by percentage for A/B tests and gradual rollouts
- enforce daily, weekly, or monthly request and cost limits using the
  authenticated key's usage buckets
- call a different model and choose its routing mode, provider preference, and
  fallback policy
- enable cache-aware and session-aware provider affinity
- attach to one or more inference API keys

Conditions expose true and false outputs. Rate and budget nodes expose within
and exceeded outputs. Percentage selection is deterministic for a session or
prompt-cache key, so a rollout does not randomly move the same cached
conversation between branches.

Saving creates an immutable draft version. Deploying a selected version copies
that snapshot to the Gateway and invalidates policy caches for attached keys.
Earlier versions remain available for rollback. Operational provider-health
recommendations appear under **Insights**, separately from the flow editor.

Custom metadata is available on the OpenAI-compatible text inference surfaces:

```json
{
  "model": "openai/gpt-5-mini",
  "metadata": {
	    "customer_plan": "pro",
	    "workspace": "acme"
  }
}
```

## Fallback behavior

If a provider returns errors or rate limits, the Gateway can retry or route to another provider that supports the same model. You should still handle 429 and 5xx responses with exponential backoff.

Model nodes in a dynamic route can also define an ordered list of fallback
models. Phaseo first exhausts the eligible provider attempts for the selected
model. If the resulting response is retryable (`429`, `500`, `502`, `503`, or
`504`), the Gateway reruns the complete policy and provider-selection pipeline
for each fallback model in order. Client errors are returned immediately and do
not switch models.

Each fallback is independently checked against workspace model restrictions,
guardrails, provider policy, pricing, and capability support. A route can store
up to eight fallback models.

Read more in:

- [Rate limits](../api-reference/limits.mdx)
- [Error handling](../api-reference/errors.mdx)

## Cache-aware and session-aware affinity

Cache-aware routing is enabled by default on text generation endpoints. After a
provider reports a real prompt-cache read, Phaseo pins matching context back to
that provider for 15 minutes, provided the provider remains healthy and allowed
by the active route, preset, guardrail, and request policy.

When `session_id` is present, the cache affinity is keyed to the session instead
of only the opening context. Session affinity is refreshed when Phaseo observes
another cache read and is retained for up to 24 hours of session activity.
Circuit breakers and policy filters always take precedence over affinity.

Opt out for one request without changing workspace or dynamic-route defaults:

```json
{
  "model": "openai/gpt-5",
  "session_id": "support-session-42",
  "provider": {
    "cache_aware_routing": false
  }
}
```

To retain context cache affinity but ignore the session identifier for one
request, use:

```json
{
  "model": "openai/gpt-5",
  "session_id": "support-session-42",
  "routing": {
    "session_affinity": false
  }
}
```

## BYOK considerations

Use BYOK when you want Phaseo routing and observability while the selected provider bills model usage to your own account.

- The first 250,000 completed BYOK requests in each UTC calendar month have no Phaseo service fee.
- After that allowance, Phaseo charges 2.5% of provider-equivalent cost.
- Keep at least $1 of Phaseo credit available. This protects managed fallback and post-allowance fee collection; provider charges still go directly to your provider account.
- Your provider's quotas, data policy, model access, and account restrictions still apply.

Provider credentials are encrypted with AES-256-GCM and bound to their workspace and provider before storage. Scope each key to the models and Phaseo API keys that need it, and disable managed fallback if a request must never use Phaseo credits.

## What to log

For production workloads, log request ids, response status codes, and model ids so you can correlate failures and confirm routing behavior when debugging.

## Related guides

- [Presets](./presets)
- [Feature Parity Matrix](../migration-guides/feature-parity-matrix)
