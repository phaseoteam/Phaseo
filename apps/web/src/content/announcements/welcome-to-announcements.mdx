---
title: Welcome to the Local Blog
description: This local MDX-powered blog section lives directly inside the web app.
publishedAt: 2026-04-19
author: Phaseo Team
tags:
  - Blog
  - MDX
  - Web
coverImage: /blog-images/welcome-grid.svg
draft: false
---

This is your new blog section, fully local to the repository.

You can write normal markdown, embed local images, and include JSX components directly in each post.

![Announcement preview image](/blog-images/welcome-grid.svg)

## Built for quick writing

- Add a new `.mdx` file in `apps/web/src/content/announcements`.
- Add images to `apps/web/public/announcements`.
- Open `/blog` in the app.

<Callout title="Tip" tone="success">
Keep frontmatter short and clear. Your `description` is used on the blog index cards.
</Callout>

## React in MDX

You can render interactive components from inside MDX:

<Counter initial={2} />

You can also use JSX expressions inline:

<ul>
	{["Local files", "Image support", "React components"].map((feature) => (
		<li key={feature}>{feature}</li>
	))}
</ul>
