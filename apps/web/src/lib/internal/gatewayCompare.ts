export type CompareTarget = "phaseo" | "openrouter" | "llmgateway" | "vercel-ai-gateway";
export type FrameTrace = { atMs: number; event: string | null; object: string | null; type: string | null; dataKind: "json" | "done" | "non-json"; dataPreview: string | null; hasContent: boolean; contentLength: number; hasUsage: boolean; isDone: boolean; rawBytesSeen: number };
export type Stats = { min: number; p50: number; avg: number; max: number };
export type GatewayStageBreakdown = { beforeMs: number | null; protocolDetectMs: number | null; irDecodeMs: number | null; executeGuardCandidatesMs: number | null; executeFilterModalitiesMs: number | null; executeRankProvidersMs: number | null; attemptBreakerMs: number | null; attemptLoadPricecardMs: number | null; attemptResolveExecutorMs: number | null; attemptNormalizeIrMs: number | null; attemptRequestBuildMs: number | null; attemptUpstreamHeadersMs: number | null; headersToFirstByteMs: number | null; headersToFirstContentMs: number | null; accountedHeadersMs: number | null; unaccountedHeadersMs: number | null };
export type GatewayStageSummary = { [K in keyof GatewayStageBreakdown]: Stats | null };
export type CompareTraceResult = { target: CompareTarget; run: number; ok: boolean; status: number; headersMs: number; firstByteMs: number | null; firstFrameMs: number | null; firstJsonFrameMs: number | null; firstContentMs: number | null; firstUsageMs: number | null; doneMs: number | null; totalMs: number; bodyBytes: number; chunkCount: number; frameCount: number; contentFrameCount: number; firstFrames: FrameTrace[]; serverTiming: Record<string, number> | null; gatewayStageBreakdown: GatewayStageBreakdown | null; error: string | null };
export type CompareSummary = { target: CompareTarget; successes: number; failures: number; headersMs: Stats | null; firstByteMs: Stats | null; firstContentMs: Stats | null; totalMs: Stats | null; stageSummary: GatewayStageSummary | null };
