{
	"name": "@ai-stats/py-sdk",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"postinstall": "node ../scripts/postinstall-viewer-prompt.cjs @ai-stats/py-sdk",
		"build": "python -m build --sdist --wheel",
		"smoke:chat": "dotenv -e .env.local -- python tests/smoke_chat.py",
		"smoke:responses": "dotenv -e .env.local -- python tests/smoke_responses.py"
	}
}
