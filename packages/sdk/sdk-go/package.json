{
  "name": "@phaseo/go-sdk",
  "version": "2.0.4",
  "private": true,
  "description": "Phaseo Go SDK (preview) - generated client with models wrapper",
  "license": "MIT",
  "scripts": {
    "postinstall": "node ../scripts/postinstall-viewer-prompt.cjs @phaseo/go-sdk",
    "test": "go test ./...",
    "smoke:chat": "dotenv -e .env.local -- go test ./tests -run TestSmokeChat -v",
    "smoke:responses": "dotenv -e .env.local -- go test ./tests -run TestSmokeChat -v",
    "smoke:responses:sdk": "dotenv -e .env.local -- go test ./tests -run TestSmokeResponsesSDK -count=1 -v"
  }
}
