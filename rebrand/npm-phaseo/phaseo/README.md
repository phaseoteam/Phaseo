# Phaseo

Phaseo is the new name for AI Stats.

This package reserves the `phaseo` npm name and re-exports the current Phaseo TypeScript SDK, which is still published as `@ai-stats/sdk` during the package migration.

```bash
npm install phaseo
```

For the scoped SDK package, use:

```bash
npm install @phaseo/sdk
```
