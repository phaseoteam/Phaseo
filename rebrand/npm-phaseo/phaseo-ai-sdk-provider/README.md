# Phaseo AI SDK Provider

Phaseo is the new name for AI Stats.

This package reserves `@phaseo/ai-sdk-provider` and re-exports the current Vercel AI SDK provider from `@ai-stats/ai-sdk-provider` during the package migration.
