# Phaseo TypeScript SDK

Phaseo is the new name for AI Stats.

This package reserves `@phaseo/sdk` and re-exports the current SDK implementation from `@ai-stats/sdk` during the package migration.

```bash
npm install @phaseo/sdk
```
